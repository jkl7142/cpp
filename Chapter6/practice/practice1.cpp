#include <iostream>
using namespace std;

int main(int argc, char* argv[]) {

    // 파생 형식 인스턴스를 기본 형식에 대한 포인터로 포인팅하고 호출한다면
    // 기본 형식과 파생 형식 중 어느 클래스의 메소드가 호출되는가?
    // => 동적 생성된 실 형식이 파생 클래스여도 참조 형식인 기본 클래스의 메소드가 호출된다.

    return 0;
}