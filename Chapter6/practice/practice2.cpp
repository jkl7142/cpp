#include <iostream>
using namespace std;

int main(int argc, char* argv[]) {

    // A는 B, B는 C의 기본 클래스일 때 C 클래스의 인스턴스를 선언한다고 가정하면
    // 가장 먼저 '실행'되는 생성자는 어느 클래스의 생성자인가?
    // => 함수 호출은 C 클래스가 먼저 호출되지만, 실행은 A 클래스가 가장 먼저 실행된다.

    return 0;
}